/**
 * lib/civic/index.ts — barrel export for the Civic Intelligence layer.
 *
 * Import civic utilities from here rather than from individual files
 * to keep import paths stable as the module grows.
 */
export * from "./age";
